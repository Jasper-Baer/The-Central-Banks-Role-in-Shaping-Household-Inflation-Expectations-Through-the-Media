# -*- coding: utf-8 -*-
"""
Created on Sat Nov 12 13:49:54 2022

@author: jbaer
"""

#model =  BertForSequenceClassification.from_pretrained(word_embedding, num_labels=3)
#model = BERT_LSTM()

# from transformers import BertTokenizer

# bert_type = 'Sequence'
# model.load_state_dict(torch.load(PATH))    

# tokenizer = BertTokenizer.from_pretrained(word_embedding, do_lower_case=True)

# data_sent = pd.read_csv('D:\Studium\PhD\Single Author\Data\dpa_sents_v01.csv')

# idx = data_news['index']

# dates = data_sent.iloc[idx][['year', 'month', 'day']]

# data_news['year'] = list(dates['year'])
# data_news['month'] = list(dates['month'])
# data_news['day'] = list(dates['day'])

# data_final['Label'] = list(data_dir["Label"])

